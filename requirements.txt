# ================================
# CRITICAL: Version matching with training environment
# ================================

# Core ML Framework (match training versions)
tensorflow==2.15.0
numpy==1.24.3
protobuf==4.23.4

# Image Processing (match training versions)
Pillow==10.0.0

# Streamlit and Web Framework
streamlit==1.48.1
toml==0.10.2

# API and Environment
python-dotenv==1.0.0
requests==2.31.0

# Additional utilities
matplotlib==3.8.0
seaborn==0.13.0
scikit-learn==1.3.0

openai>=1.0.0

# ================================
# Installation Instructions:
# ================================
# OPTION 1 - Install in stages (RECOMMENDED):
#    pip install --upgrade pip
#    pip install numpy==1.24.3
#    pip install tensorflow==2.15.0
#    pip install streamlit==1.28.0
#    pip install Pillow==10.0.0 python-dotenv==1.0.0 requests==2.31.0
#    pip install matplotlib==3.8.0 seaborn==0.13.0 scikit-learn==1.3.0
#
# OPTION 2 - Install all at once:
#    pip install --upgrade pip
#    pip install -r requirements.txt
#
# OPTION 3 - If still having issues, use offline/cached mode:
#    pip install --no-index --find-links=<local_wheel_dir> -r requirements.txt
#
# 3. Run the application:
#    streamlit run app.py
# ================================
